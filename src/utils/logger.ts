// import {logMessage, ERROR, INFO} from 'react-native-embrace';

export const logger = {
    // info: (message) => {
    //     _log(message, INFO);
    // },
    // error: (message) => {
    //     _log(message, ERROR);
    // }
};

// const _log = (message, type) => logMessage(message, type);
